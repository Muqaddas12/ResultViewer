import { registerRootComponent } from 'expo';
import ResultViewer from './ResultViewer';

// Register the root component (no need to reference AppRegistry directly in this case)
registerRootComponent(ResultViewer);
